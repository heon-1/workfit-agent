# utils/helpers.py

# 프로젝트 전반에 걸쳐 사용될 수 있는 작고 재사용 가능한 함수들을 여기에 정의합니다.
# 예시:
def clean_text(text: str) -> str:
    """텍스트에서 불필요한 공백이나 특정 문자를 제거합니다."""
    # 간단한 예시: 양쪽 공백 제거
    return text.strip()

# 필요에 따라 다양한 헬퍼 함수 추가 